spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: currency-exchange-service
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto=update
server:
  port: 8000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka




